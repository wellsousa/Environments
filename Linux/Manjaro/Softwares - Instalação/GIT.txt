 
Para instalar o GIT use o comando:

sudo pacman -S git

Para configurar um usuario GLOBAL:

git config --global user.name seu_usuario

git config --global user.email seu_email

Para configurar um usuário por projeto ou LOCAL:

git config --local user.name seu_usuario

git config --local user.email seu_email
